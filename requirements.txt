# =====================================================================
# Direct dependencies of the ML vs GNN pipeline (run_pipeline.py)
#
# Python version: >= 3.11   (scipy 1.16 and contourpy 1.3 require it)
#   -> write the exact tested version in the README (python --version)
#
# Versions below were taken from the `pip list` of the environment used for the paper
# runs (fda_pipeline_env). The complete, exact environment of the final clean run lives in
# requirements-lock.txt (pip freeze) -- regenerate it after a successful clean run.
# =====================================================================

# -- Core data science ------------------------------------------------
numpy>=1.26.0,<2.0.0          # <2.0: required by the torch/dgl builds used for the GNN baselines
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.6.0
joblib==1.5.2

# -- Classical ML (steps 01-04) ---------------------------------------
lightgbm==4.6.0
xgboost==3.1.2

# -- Cheminformatics ---------------------------------------------------
rdkit==2025.9.3

# -- Docking (steps 06-07): Python bindings, no external binary needed --
vina==1.2.7
meeko==0.8.0                  # API changed across versions (PDBQTWriterLegacy)

# -- GNN baseline (step 09) --------------------------------------------
torch==2.5.1
lightning==2.6.5
pytorch-lightning==2.6.5       # installed alongside lightning in the tested environment
chemprop==2.2.4                # v2 API

# -- ADMET data loading (step 08 uses `from tdc.single_pred import ADME, Tox`) --
# PyTDC itself is NOT listed here: it declares rdkit-pypi (an old RDKit 2022.9 build) as a
# dependency, which overwrites the pinned RDKit. It is installed WITHOUT dependencies by
# setup_env.sh:  pip install --no-deps PyTDC==0.4.1
# Its real import-time needs are listed below.
fuzzywuzzy==0.18.0
huggingface_hub==0.36.2
setuptools==69.5.1            # PyTDC imports pkg_resources, removed from newer setuptools

# -- Visualization -----------------------------------------------------
matplotlib==3.10.8
seaborn==0.13.2
adjustText==1.4.0

# =====================================================================
# Packages that were in the original requirements but are NOT imported by any script
# (checked with: grep -nE "^\s*(import|from)\s+(tdc|pubchempy|gemmi|loguru)" scripts/*.py):
#   PubChemPy (also needs network access), gemmi, loguru
# =====================================================================

# Removed on purpose (do not add back):
#   nvidia-nccl-cu12 (installed automatically by xgboost 3.x on Linux; pinning it breaks macOS/Windows),
#   litlogger, litmodels                         -> pulled in by lightning/torch when needed
#   pillow, contourpy, cycler, fonttools, kiwisolver, pyparsing, packaging, python-dateutil,
#   pytz, six, threadpoolctl, tzdata             -> transitive; they belong in requirements-lock.txt
#   dgl, dgllife, torchdata                      -> AttentiveFP was removed (dgl 2.1.0 does not support torch 2.5.1)
